node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Capacitor / Android — the native project is committed; its own
# android/.gitignore excludes build artifacts (build/, .gradle, *.apk, ...)
ios
*.apk
.capacitor
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Signing (kept private via GitHub Secrets; CI writes these at build time)
android/app/keystore.properties
*.keystore
